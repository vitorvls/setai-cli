# Forbidden Libraries

## Objective

Explicit deny-list when provided by the user. SetAI does **not** invent a deny-list.

{{FORBIDDEN_LIBRARIES}}

{{#if FORBIDDEN_LIBRARIES_CUSTOM}}
## User-declared forbidden

{{FORBIDDEN_LIBRARIES_CUSTOM}}
{{/if}}

## Notes

- Absence of a package from allowed-libs does not automatically mean it is forbidden
- Do not claim that CLIs never perform network I/O when AI provider SDKs (or other HTTP clients) are dependencies

## Related

- Allowed: `.cursor/libs/allowed-libs.md`
