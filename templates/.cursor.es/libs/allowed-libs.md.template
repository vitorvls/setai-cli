# Allowed Libraries

## Objective

Libraries evidenced in `package.json` (classified when known). Do not invent packages.

## From repository evidence

{{ALLOWED_LIBRARIES}}

{{#if ALLOWED_LIBRARIES_CUSTOM}}
## User-declared additions

{{ALLOWED_LIBRARIES_CUSTOM}}
{{/if}}

## Policy

- Prefer packages already listed above
- Adding a new dependency requires an explicit decision
- Optional AI SDKs are for `--beta` enrichment only — core must work without them

## Related

- Tech stack: `.cursor/context/tech-stack.md`
- Forbidden: `.cursor/libs/forbidden-libs.md`
